---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<section class="hero-section">
  <div class="hero-content">
    <div class="hero-tagline" role="heading" aria-level="1">The AI-Native<br/>Bitemporal Database</div>
    <p class="hero-subtitle">
      Append-only immutable storage with MVCC snapshot reads, full SQL,
      LSM-tree engine, and embedded AI runtime. Built in Rust.
    </p>
    <div class="hero-stats">
      <div class="hero-stat">
        <span class="hero-stat-value">276<span class="hero-stat-unit">ns</span></span>
        <span class="hero-stat-label">Point Reads</span>
      </div>
      <div class="hero-stat">
        <span class="hero-stat-value">1.9<span class="hero-stat-unit">µs</span></span>
        <span class="hero-stat-label">Point Writes</span>
      </div>
      <div class="hero-stat">
        <span class="hero-stat-value">4<span class="hero-stat-unit">×</span></span>
        <span class="hero-stat-label">Faster reads vs SQLite</span>
      </div>
      <div class="hero-stat">
        <span class="hero-stat-value">20<span class="hero-stat-unit">×</span></span>
        <span class="hero-stat-label">Faster writes vs SQLite</span>
      </div>
    </div>
    <div class="hero-cta">
      <a href={`${base}/getting-started/quickstart/`}>Get Started</a>
      <a href={`${base}/sql/`} class="secondary">SQL Reference</a>
    </div>
  </div>
</section>

<style>
  .hero-section {
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 5rem 2rem 4rem;
    background: var(--tensor-dark);
  }
  .hero-content {
    max-width: 720px;
  }
  .hero-tagline {
    font-size: clamp(2rem, 5vw, 3.2rem);
    font-weight: 700;
    line-height: 1.15;
    margin: 0 0 1.25rem;
    background: var(--tensor-gradient);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  .hero-subtitle {
    font-size: 1.15rem;
    color: var(--tensor-text-muted);
    line-height: 1.7;
    margin: 0 auto 2.5rem;
    max-width: 560px;
  }
  .hero-stats {
    display: flex;
    gap: 2.5rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 2.5rem;
  }
  .hero-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  .hero-stat-value {
    font-size: 2.2rem;
    font-weight: 700;
    color: var(--tensor-text);
    line-height: 1;
    font-variant-numeric: tabular-nums;
  }
  .hero-stat-unit {
    font-size: 1.2rem;
    font-weight: 500;
    opacity: 0.6;
  }
  .hero-stat-label {
    font-size: 0.78rem;
    font-weight: 500;
    color: var(--tensor-text-faint);
    margin-top: 0.35rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  .hero-cta {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  .hero-cta a {
    display: inline-flex;
    align-items: center;
    padding: 0.7rem 1.5rem;
    border-radius: 8px;
    font-weight: 600;
    font-size: 0.9rem;
    text-decoration: none;
    background: var(--tensor-gradient);
    color: #ffffff;
    transition: transform 0.2s, box-shadow 0.2s;
  }
  .hero-cta a:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 16px rgba(59, 130, 246, 0.3);
  }
  .hero-cta a.secondary {
    background: transparent;
    border: 1px solid var(--tensor-card-border);
    color: var(--tensor-text);
  }
  .hero-cta a.secondary:hover {
    border-color: var(--tensor-blue);
    box-shadow: none;
  }
  @media (max-width: 480px) {
    .hero-section { padding: 3rem 1.5rem 2.5rem; }
    .hero-stat-value { font-size: 1.8rem; }
    .hero-stat-unit { font-size: 1rem; }
    .hero-cta { flex-direction: column; align-items: center; }
  }
</style>
